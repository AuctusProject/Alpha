import { UsernameRegistrationDirective } from './username-registration.directive';

describe('UsernameRegistrationDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameRegistrationDirective(null);
    expect(directive).toBeTruthy();
  });
});
