import { EmailRegistrationDirective } from './email-registration.directive';

describe('EmailRegistrationDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailRegistrationDirective(null);
    expect(directive).toBeTruthy();
  });
});
